{
  "name": "pie",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -o build.js",
    "watch": "watchify index.js -o build.js --debug --verbose",
    "serve": "http-server -s",
    "start": "npm run watch & npm run serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.0.0",
    "http-server": "^0.8.5",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "amcharts": "^3.14.5",
    "c3": "^0.4.10",
    "d3": "^3.5.15",
    "nvd3": "^1.8.2"
  }
}
